export const namespace = 'session';

export const TOKEN_REFRESH_INTERVAL = 1000 * 60 * 60 * 12;

export const RECONNECT_INTERVAL = 10 * 1000;

export const MAX_CONNECT_COUNT = 10;

export const DEFAULT_SYSTEM = {
  commentLength: 500,
  imageMaxNumber: 9,
  imageMaxSize: 5,
  videoMaxNumber: 1,
  videoMaxSize: 30,
  videoMaxTime: 30,
};
